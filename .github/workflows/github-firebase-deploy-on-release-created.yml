# This workflow will run tests using node and then publish a package to GitHub Packages when a release is created
# For more information see: https://help.github.com/actions/language-and-framework-guides/publishing-nodejs-packages

name: On release created - Firebase deploy PROD

on:
  release:
    types: [published]

# on:
#   push:
#     tags:
#       - v*

env:
  SYS_ADMIN_EMAIL: 'manuel@aconcagua.finance'
  NEW_USERS_TEMP_PASSWORD: '@Temp1234'

  TEST_ENV_KEY: 'TEST_ENV_VALUE'
  ENVIRONMENT: prod

  FIREB_PROJECT_ID: 'aconcagua-365314'
  FIREB_API_KEY: 'AIzaSyCVjVkwUc63D9ES6AVwPCEahJHw1ky978Q'
  FIREB_AUTH_DOMAIN: 'aconcagua-365314.firebaseapp.com'
  FIREB_STORAGE_BUCKET: 'aconcagua-365314.appspot.com'
  FIREB_MESSAGING_SENDER_ID: 101110014661
  FIREB_APP_ID: '1:101110014661:web:137167add231676d4846ac'
  FIREB_MEASURAMENT_ID: 'G-4EP13854VS'

  AIRTABLE_API_KEY: '??'

  GOOGLE_OAUTH_CLIENT_ID: ${{secrets.GOOGLE_OAUTH_CLIENT_ID_PROD}}
  GOOGLE_OAUTH_CLIENT_SECRET: ${{secrets.GOOGLE_OAUTH_CLIENT_SECRET_PROD}}
  GOOGLE_OAUTH_REDIRECT_URL: 'https://us-central1-aconcagua-365314.cloudfunctions.net/googleOAuth/oauth2callback'
  GOOGLE_CALENDAR_EVENT_WEBHOOK_URL: 'https://us-central1-aconcagua-365314.cloudfunctions.net/userCalendarEvents/google-event-webhook'

  DOLAR_HOY_DOLAR_CRIPTO_DOM_QUERY: 'Dólar Bolsa'
  API_USD_VALUATION: 'http://us-central1-aconcagua-365314.cloudfunctions.net/scrapper/dolar-cripto'
  API_TOKENS_VALUATIONS: 'http://us-central1-aconcagua-365314.cloudfunctions.net/market/tokensQuotes'
  API_EVALUATE_VAULTS: 'http://us-central1-aconcagua-365314.cloudfunctions.net/vaultsPolygon/evaluate'
  API_VAULT_ADMIN: 'http://us-central1-aconcagua-365314.cloudfunctions.net/vaultsPolygon/vaultAdmin'

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    permissions: write-all

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 16.17.1
          registry-url: https://npm.pkg.github.com/
      - run: npm ci --force --legacy-peer-deps
        env:
          NODE_AUTH_TOKEN: ${{secrets.VS_GITHUB_NPM_TOKEN}}
          # NODE_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}} este no tiee permisos en otro repo
          VS_GITHUB_NPM_TOKEN: ${{secrets.VS_GITHUB_NPM_TOKEN}}
      - run: npm run prettier-ci
      - run: npm run lint-ci
      - run: npm run test-ci

      # ya lo hace el firebase.json
      # - run: npm run build

      - run: npm run deploy-ci
        env:
          NODE_AUTH_TOKEN: ${{secrets.VS_GITHUB_NPM_TOKEN}}
          VS_GITHUB_NPM_TOKEN: ${{secrets.VS_GITHUB_NPM_TOKEN}}
          FIREBASE_TOKEN: ${{secrets.FIREBASE_TOKEN}}

  # publish-gpr:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #     packages: write
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: actions/setup-node@v2
  #       with:
  #         node-version: 14
  #         registry-url: https://npm.pkg.github.com/
  #     - run: npm ci

  #       env:
  #         NODE_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
